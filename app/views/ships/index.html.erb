<!DOCTYPE html>
<html>
<head>
  <title></title>
</head>
<body>

  <h4>配送先登録/一覧</h4>

  <%= form_for(@ship) do |f| %>


    <div class="field row">
      <tr>
        <td><%= f.label :code, "郵便番号(ハイフンなし)", class: "col-xs-offset-1 col-sm-3" %></td>
        <td><p><%= f.text_field :code, class: "col-xs-2" %></p></td>
      </tr>
    </div>

    <div class="field row">
      <%= f.label :address, "住所", class: "col-xs-offset-1 col-xs-3" %>
      <p><%= f.text_area :address, class: "col-xs-3" %></p>
    </div>

    <div class="field row">
      <%= f.label :name, "宛先", class: "col-xs-offset-1 col-xs-3" %>
      <p><%= f.text_field :name, class: "col-xs-2" %></p>
    </div>

    <div class="action row">
      <%= f.submit '登録する', class: "btn btn-primary col-xs-offset-8 col-xs-1" %>
      </div>
  <% end %>

</body>
</html>